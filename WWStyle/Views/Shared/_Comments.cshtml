@model List<Comment>

@foreach (var comment in Model)
{
    <div class="comment">
        <div class="user">@comment.User.UserName</div>
        <div class="text">@comment.Text</div>
        <div class="date">@comment.CreateDate.ToString("yyyy-MM-dd HH:mm")</div>
    </div>
}